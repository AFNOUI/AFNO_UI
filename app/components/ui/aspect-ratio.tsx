import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

export const AspectRatio = AspectRatioPrimitive.Root;

/*
 * ============================================
 * TAILWIND CSS VARIABLES DOCUMENTATION
 * ============================================
 * This component is a thin wrapper around Radix UI's AspectRatio primitive.
 *
 * Variables Used:
 * --------------
 *   None
 *
 * Line 3 - `const AspectRatio = AspectRatioPrimitive.Root;`
 *   This component forwards directly to the Radix primitive and does not
 *   apply any Tailwind classes or Tailwind CSS variables in this file.
 */
